//! `zeck-research` — autoresearch-style parameter-sweep harness.
//!
//! Skeleton entry point. Real CLI + evaluator wiring lands in later steps.

fn main() {
    eprintln!("zeck-research: skeleton — CLI not yet implemented");
    std::process::exit(2);
}
